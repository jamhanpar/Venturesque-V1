import React from 'react';

const Testing = (props) => {
    return (
        <div>
        </div>
    )
}

// const msp = () => ({});

// const mdp = dispatch => {};

export default connect(null, null)(Testing);